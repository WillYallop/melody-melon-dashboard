import trackNavigation from './modules/trackNavigation.js'
import navigation from './modules/navigation.js'

const state = () => ({
  
});

const mutations = {
  
};

const actions = {
  
};

const modules = {
    trackNavigation,
    navigation
};

export default {
    state,
    mutations,
    actions,
    modules,
}

